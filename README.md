# Coral (Static)

This project now runs as a plain **HTML/CSS/JavaScript** app (no TypeScript, TSX, or Vite).

## Run locally

```bash
npm run dev
```

Then open:

- http://localhost:3000/

## GitHub Pages

Deployment is handled by `.github/workflows/deploy-pages.yml`, which publishes static files directly.
